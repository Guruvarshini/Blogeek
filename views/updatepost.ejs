<!-- addbook.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Book</title>
    <style>
        body {
            width: 100%;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        
        form {
            background-color: #fff;
            padding: 10px;
            border-radius: 8px;
            box-shadow: 0 0 10px  rgba(0, 0, 0, 0.1);
            width: 100%;
            box-sizing: border-box;
        }
        .full{
            width: 100%;
        }
        label {
            display: block;
            margin-bottom: 8px;
            color: #333;
        }

        input,
        textarea {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #007bff;
            color: #fff;
            padding: 10px 15px;
            margin-left: 50%;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }
        
        h1 {
            text-align: center;
            color: #007bff;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <!-- updatepost.ejs -->

<div class="full">
    <%-include("../views/partials/header.ejs")%>
    <h1 style="color: #007bff;">Update Post</h1>

    <form action="/update-post/<%=post.postId%>" method="post">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= post.tit %>" required>

        <label for="category">Category:</label>
        <input type="text" id="coverUrl" name="category" value="<%= post.cat %>" required>

        <label for="content"> Updated Content:</label>
        <textarea id="content" name="content" required><%= post.cont %></textarea>

        <button type="submit">Update Post</button>
    </form>

    <%-include("../views/partials/footer.ejs")%>
</div>

</body>

</html>
